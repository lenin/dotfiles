alias vi=vim

# Git
alias g='git status'
alias gd='git diff'
alias gds='git diff --staged'
alias gci='git commit -v'
alias gl='git log'
alias gcd='git checkout develop && git pull'
alias gpdm='git checkout master && git pull && git merge develop && git push && git checkout develop'
